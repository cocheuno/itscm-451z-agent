{
  "name": "find_similar_incidents",
  "description": "Find recently resolved incidents whose short description is similar to the current one. Returns number, category, assignment group, and resolution notes. Use to inform routing; do not copy resolutions blindly.",
  "action_tier": "read",
  "input_schema": {
    "type": "object",
    "properties": {
      "text": {"type": "string", "description": "The incident short description or a distilled symptom phrase."},
      "limit": {"type": "integer", "minimum": 1, "maximum": 10, "default": 5}
    },
    "required": ["text"],
    "additionalProperties": false
  }
}
